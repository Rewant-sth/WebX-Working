import CustomizeTripForm from '@/components/customize-trip/CustomizeTripForm';

export default function CustomizeTripPage() {
    return (
        <div className="min-h-screen bg-gray-50">
            <CustomizeTripForm />
        </div>
    );
}
